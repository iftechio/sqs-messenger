{
  "name": "@ruguoapp/sqs-messenger",
  "version": "2.7.1",
  "description": "",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "compile": "tsc",
    "clean": "rm -rf dist",
    "purge": "rm -rf package-lock.json node_modules && npm run clean",
    "test": "rm -rf dist && npm run lint && tsc && AWS_ACCESS_KEY_ID=ABCDEF AWS_SECRET_ACCESS_KEY=ABCDEF ava -T 10000 dist/tests",
    "lint": "tslint --project .",
    "prettier": "prettier --config .prettierrc --write '{lib,tests,examples}/**/*.ts' index.ts",
    "prepublishOnly": "npm run compile"
  },
  "dependencies": {
    "@types/bluebird": "3.5.20",
    "@types/node": "8.5.1",
    "bluebird": "3.5.1",
    "debug": "3.1.0"
  },
  "peerDependencies": {
    "aws-sdk": "^2.244.1"
  },
  "devDependencies": {
    "@types/sinon": "4.3.3",
    "ava": "^0.25.0",
    "aws-sdk": "2.244.1",
    "sinon": "5.0.7",
    "source-map-support": "0.5.6",
    "tslint": "^5.18.0",
    "tslint-config-prettier": "1.13.0",
    "tslint-jike-node": "0.0.14",
    "typescript": "2.8.3"
  },
  "author": "",
  "license": "ISC",
  "publishConfig": {
    "registry": "http://npmjs.ruguoapp.com"
  }
}
